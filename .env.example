# Configuración del modelo Whisper
# Opciones: tiny.en, base.en, small.en, medium.en, large
# tiny.en = más rápido, menos preciso (recomendado para Render)
# base.en = balance calidad/velocidad
WHISPER_MODEL=base.en

# Token de autenticación para workers
# IMPORTANTE: Cambia esto en producción a un valor secreto fuerte
# Genera uno con: python -c "import secrets; print(secrets.token_urlsafe(32))"
WORKER_API_KEY=change-me-in-production

# URL del backend para el worker local (sin barra final)
API_URL=https://traductor-videos-en-es.onrender.com
