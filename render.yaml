services:
  - type: web
    name: traductor-videos-web
    runtime: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: WORKER_API_KEY
        sync: false
      - key: WHISPER_MODEL
        value: tiny.en
